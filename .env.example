# Local dev only — production values are set as Worker secrets via
# `wrangler secret put <NAME>`, never committed. Copy this file to `.env`
# for `wrangler dev` / the seed script to pick up locally.

SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_VERSION=2026-06-01
SANITY_WRITE_TOKEN=
